/**
 * Individual sorted_events_descending module.
 * Provides the computor for the sorted_events_descending graph node.
 */

const { computor } = require("./wrapper");

module.exports = {
    computor,
};
