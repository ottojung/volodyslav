/**
 * Individual sorted_events_ascending module.
 * Provides the computor for the sorted_events_ascending graph node.
 */

const { computor } = require("./wrapper");

module.exports = {
    computor,
};
