/**
 * Individual all_events module.
 * Provides the computor for the all_events graph node.
 */

const { makeBox, makeComputor } = require("./wrapper");

module.exports = {
    makeBox,
    makeComputor,
};
