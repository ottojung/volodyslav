/**
 * Individual ontology module.
 * Provides the computor for the ontology graph node.
 */

const { makeBox, makeComputor } = require("./wrapper");

module.exports = {
    makeBox,
    makeComputor,
};
