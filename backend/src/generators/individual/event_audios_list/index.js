const { computeEventAudiosList, isAudioFilename } = require("./compute");
const { makeComputor } = require("./wrapper");

module.exports = {
    computeEventAudiosList,
    isAudioFilename,
    makeComputor,
};
