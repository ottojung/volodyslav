/**
 * Individual config module.
 * Provides the computor for the config graph node.
 */

const { makeBox, makeComputor } = require("./wrapper");

module.exports = {
    makeBox,
    makeComputor,
};
