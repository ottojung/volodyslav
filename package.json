{
  "name": "project-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently 'npm run dev -w frontend' 'npm run dev -w backend'"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
